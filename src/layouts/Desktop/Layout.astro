---
import GutterAds from "@components/common/wrappers/gutterAds/GutterAds.astro";
import AdContainer from "@components/common/adcontainer/AdContainer.astro";
import AdCaller from "@components/common/adcaller/AdCaller.astro";
import Header from "@components/desktop/header/Header.astro"
import Footer from "@components/desktop/footer/Footer.astro";
import Article from "@components/desktop/article/Article.astro";

const { ads, pageTarget, app } = Astro.props;

---

<>
    <!-- <GutterAds>
        <AdCaller
            pageTarget={pageTarget}
            adCode={ad.adCode}
            divId={ad.divId}
            size={ad.size}
            slot="left"
        />
        <AdCaller
            pageTarget={pageTarget}
            adCode={ad.adCode}
            divId={ad.divId}
            size={ad.size}
            slot="right"
        />
    </GutterAds> -->

    <AdContainer>
        <AdCaller
            {...ads.atf_1}
            pageTarget={pageTarget}
            inlineStyle={{margin: '5px 0'}}
        />
    </AdContainer>
    <Header pageTarget={pageTarget}/>
    <Article app={app} category={pageTarget.category} msid={pageTarget.msid} pageTarget={pageTarget} />
    <slot />
    <Footer />
</>
